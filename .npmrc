legacy-peer-deps=true

# Dependency install scripts do not run. `scripts/security/install-scripts.mjs`
# is the other half: it enumerates the scripts this skips and fails on any that
# have not been read and recorded in `install-scripts-allowlist.json`.
ignore-scripts=true
