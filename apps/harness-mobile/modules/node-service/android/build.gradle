apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'network.twistedpear.harness'
version = '0.0.0'

android {
  namespace "expo.modules.twistedpear.nodeservice"
  defaultConfig {
    versionCode 1
    versionName "0.0.0"
  }
}
