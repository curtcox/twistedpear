# SPEC-WIRE — Reticulum wire protocol (adopted)

<!-- tp-doc
lifecycle: live
audited: 2026-07-20
register: none
-->

**Group:** A (adopted) · **Status:** normative (profile) · **Migration phase:** 3

## Scope

Reticulum packet formats, announce mechanics, link establishment, and the cryptographic
constructions beneath them. Web analog: IP/TCP/TLS.

## Adoption posture

Authored upstream by the Reticulum project; byte-level compatibility with the Python
reference (RNS 0.9.4 pin, see [conformance/UPSTREAM.md](../../conformance/UPSTREAM.md))
is a permanent constraint. This document is a **profile**, not a re-specification —
upstream owns the wire format.

## Profile template (shared by all Group A specs)

A profile is one page with exactly five sections:

1. **Upstream pin** — the upstream project, version, and commit the profile is
   written against.
2. **Subset** — the upstream features/fields/flows TwistedPear uses, as a table with
   one row per item and a pointer to the vector or interop test that pins it.
3. **Extensions** — anything TwistedPear adds that upstream doesn't define (ideally
   with an upstream-publication plan, see
   [docs/upstream-publication.md](../../docs/upstream-publication.md)).
4. **Deviations** — anything TwistedPear does differently (target: an empty section;
   any entry is a bug report against either this repo or upstream).
5. **Evidence** — the conformance commands that hold the profile true in CI.

A profile is _done_ when every subset row cites at least one pinned vector or interop
test.

---

## 1. Upstream pin

| Upstream                  | Version | Role                                                                                                |
| ------------------------- | ------- | --------------------------------------------------------------------------------------------------- |
| Python RNS                | 0.9.4   | Golden vector generation ([conformance/vectors/generate.py](../../conformance/vectors/generate.py)) |
| Python RNS (docker peers) | 0.9.5   | Live interop peer ([conformance/docker](../../conformance/docker/))                                 |

Bumping either pin is a reviewed protocol event (see
[conformance/UPSTREAM.md](../../conformance/UPSTREAM.md)).

## 2. Subset

Every row cites a pinned vector in [conformance/vectors/](../../conformance/vectors/)
(regenerated by `npm run vectors:generate`) or an interop test.

| Upstream feature                           | TwistedPear use                       | Pinned by                                                                                                                                                                 |
| ------------------------------------------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SHA-256                                    | Destination/identity hashing, PKCS#7  | `crypto.json` → `sha256`                                                                                                                                                  |
| HMAC-SHA-256                               | Token authentication                  | `crypto.json` → `hmacSha256`                                                                                                                                              |
| HKDF-SHA-256                               | Key derivation                        | `crypto.json` → `hkdfSha256`, `identity.json` → `hkdf`                                                                                                                    |
| Ed25519 identity keys                      | Identity, signing                     | `identity.json` → `identities`, `signatures` (`alice-test-message`)                                                                                                       |
| X25519 + AES token                         | Encrypted delivery                    | `identity.json` → `encryption` (`alice-to-bob`, `bob-to-alice`), `token` (`fixed-iv`)                                                                                     |
| Ratchets (forward secrecy)                 | Announce ratchet, ratcheted delivery  | `identity.json` → `ratchets`, `encryption` (`bob-ratchet`)                                                                                                                |
| Destination naming/hashing                 | Single/group destination addressing   | `packet.json` → `destinations` (`single-example-chat`, `single-example-announce`)                                                                                         |
| Packet header type 1 & 2                   | Data request, path-response transport | `packet.json` → `packets` (`header1-group-data-request`, `header2-announce-path-response`)                                                                                |
| Announce (app-data + ratchet)              | Presence/path discovery               | `packet.json` → `announces` (`alice-example-announce-app-data`, `alice-example-announce-ratchet`)                                                                         |
| Link establishment + encryption            | Established links, encrypted data     | `packages/reticulum-ts` `link.test.ts` ("Link establishment over PipeInterface"); `npm run test:interop` ("establishes a link with Python and echoes encrypted payloads") |
| Link request/response                      | App-request over links                | `link.test.ts` ("Link request/response")                                                                                                                                  |
| Link identification                        | Initiator identity reveal             | `link.test.ts` ("Link identification")                                                                                                                                    |
| Channel messaging                          | Ordered channel delivery              | `link.test.ts` ("Channel messaging")                                                                                                                                      |
| Announce/data packets over the wire (live) | Byte-level RNS interop                | `npm run test:interop` ("discovers Python announces and exchanges data packets with proofs"); TCP-flap resume slice                                                       |

## 3. Extensions

None at the wire layer. TwistedPear adds nothing to Reticulum packet, announce, link,
or crypto formats; all higher-level additions live above this layer (SPEC-MSG and
above).

## 4. Deviations

None. Any byte-level divergence from RNS 0.9.4 is a bug — either the golden vectors
(regenerated from the Python reference) or the interop matrix would fail.

## 5. Evidence

- `npm run vectors:generate` — regenerates the golden vectors from Python RNS 0.9.4;
  drift is a diff.
- Vector replay: `crypto`, `identity`, `packet` suites in
  [packages/reticulum-ts/test](../../packages/reticulum-ts/test/) and
  [packages/protocol/test](../../packages/protocol/test/) (default `vitest` run).
- `npm run test:interop` — live byte-level interop against Python RNS in Docker
  (needs Docker/Python; skip if unavailable).

## Implementations

- [packages/reticulum-ts](../../packages/reticulum-ts/) (production TypeScript)
- Python RNS (upstream reference, exercised in Docker interop)
- Simulator transport models
  ([packages/effects](../../packages/effects/) sim adapters)
