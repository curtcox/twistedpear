# Minimal i2pd config for Reticulum SAM interop (Phase 2 M7).
# SAM listens on all interfaces so peer containers can reach the bridge.

[sam]
enabled = true
address = 0.0.0.0
port = 7656
portudp = 7655

[log]
loglevel = info
